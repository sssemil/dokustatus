NEXT_PUBLIC_API_BASE=http://localhost:3001
# Main domain for the reauth product (defaults to reauth.dev if not set)
# Set to reauth.test for local development
NEXT_PUBLIC_MAIN_DOMAIN=reauth.dev
# Umami analytics website ID (only set in production builds)
# NEXT_PUBLIC_UMAMI_WEBSITE_ID=
